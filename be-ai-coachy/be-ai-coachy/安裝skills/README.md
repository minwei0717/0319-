# Claude Code 全局配置倉庫

這是 Jimmy 的 Claude Code 全局配置倉庫，包含可跨專案共用的技能、代理、指令等。

## 📁 目錄結構

```
.claude/
├── skills/              # 通用技能（所有專案可用）
├── agents/              # 通用代理配置
├── commands/            # 自訂指令
├── hooks/               # Git hooks 模板
├── docs/                # 文件資源
│   └── workspace/       # 工作區配置指南和工具
│       ├── templates/   # 專案模板
│       └── scripts/     # 初始化腳本
├── lib/                 # 共享程式庫
├── mcp-servers/         # MCP 伺服器配置
├── plugins/             # 插件
├── CLAUDE.md            # 全局指引
├── settings.json        # 全局預設設定
└── .mcp.json            # MCP 配置
```

### 為新專案初始化配置

#### 方法 1：使用初始化腳本（推薦）

```bash
# Linux/macOS
~/.claude/docs/workspace/scripts/init-project-claude.sh ~/projects/my-new-project "My New Project"

# Windows (PowerShell)
~\.claude\docs\workspace\scripts\init-project-claude.ps1 -ProjectPath "C:\projects\my-new-project" -ProjectName "My New Project"
```

#### 方法 2：手動複製模板

```bash
# 複製模板到專案
cp -r ~/.claude/docs/workspace/templates/project-claude-template/* ~/projects/my-project/.claude/

# 編輯 CLAUDE.md
vim ~/projects/my-project/.claude/CLAUDE.md

# 提交到版控
cd ~/projects/my-project
git add .claude/
git commit -m "docs: 初始化 Claude Code 配置"
```

## 📚 配置層級

Claude Code 會按以下優先順序載入配置（後者覆蓋前者）：

1. **全局配置**（此倉庫）：`~/.claude/settings.json`
   - 所有專案的預設設定
   - 通用技能、代理、指令

2. **專案配置**：`<project>/.claude/settings.json`
   - 專案特定的設定覆蓋
   - 專案特定的技能和指引

3. **本地覆蓋**：`<project>/.claude/settings.local.json`
   - 個人本地設定（不納入版控）
   - API keys、個人偏好等

## 🛠️ 使用場景

### 單人開發，多專案

```
~/.claude/                     # 此倉庫（通用配置）
├── skills/                    # 通用技能
└── settings.json              # 全局設定

~/projects/web-app/.claude/    # 專案 A
├── CLAUDE.md                  # 「React + TypeScript 專案」
└── skills/                    # 前端特定技能

~/projects/api/.claude/        # 專案 B
├── CLAUDE.md                  # 「FastAPI 後端專案」
└── skills/                    # 後端特定技能
```

### 團隊協作

```
~/.claude/                     # 個人全局配置（此倉庫）
└── settings.local.json        # 個人 API key

~/team-project/.claude/        # 團隊專案（納入 git）
├── CLAUDE.md                  # 團隊規範和指引
├── skills/                    # 團隊共享技能
└── settings.json              # 團隊預設設定
```

## 📖 相關文件

- [docs/workspace/](docs/workspace/) - 工作區配置指南和工具
- [mcp-servers/INDEX.md](mcp-servers/INDEX.md) - MCP 服務器索引

## 🔧 維護

### 更新全局配置

```bash
cd ~/.claude
git pull
# 進行修改
git add .
git commit -m "feat: 新增 XXX 技能"
git push
```

### 同步到其他電腦

```bash
cd ~/.claude
git pull
```

## 📝 版控策略

### 納入版控（此倉庫）
- ✅ `skills/` - 通用技能
- ✅ `agents/` - 代理配置
- ✅ `commands/` - 指令
- ✅ `hooks/` - Hooks
- ✅ `templates/` - 模板
- ✅ `scripts/` - 腳本
- ✅ `CLAUDE.md` - 全局指引
- ✅ `settings.json` - 全局設定（不含敏感資訊）
- ✅ `.mcp.json` - MCP 配置

### 忽略（不納入版控）
- ❌ `debug/` - 除錯資訊
- ❌ `cache/` - 快取
- ❌ `history.jsonl` - 對話歷史
- ❌ `ide/` - IDE 狀態
- ❌ `session-env/` - 會話環境
- ❌ `todos/` - 臨時待辦
- ❌ `telemetry/` - 遙測資料
- ❌ `settings.local.json` - 本地覆蓋（敏感資料）
- ❌ `.credentials.json` - 憑證

詳見 [.gitignore](.gitignore)

## 💡 最佳實踐

1. **技能命名**：使用描述性名稱，例如 `frontend-testing.md`、`backend-api-design.md`
2. **專案 CLAUDE.md**：每個專案都應該有清楚的 CLAUDE.md 說明技術棧和規範
3. **敏感資訊**：永遠使用 `settings.local.json`，不要提交敏感資訊
4. **定期同步**：定期 `git pull` 確保配置是最新的
5. **團隊協作**：專案配置納入專案倉庫，全局配置保持個人化

## 🆘 常見問題

### Q: 如何在多台電腦間同步配置？
A: 這個倉庫就是為了解決這個問題！在每台電腦上克隆此倉庫到 `~/.claude`，然後定期 pull/push。

### Q: 專案配置和全局配置衝突怎麼辦？
A: 專案配置會覆蓋全局配置。優先順序：本地 > 專案 > 全局。

### Q: 如何與團隊共享技能？
A: 將技能放在專案的 `.claude/skills/` 目錄中，並納入專案的 git 倉庫。

### Q: 可以有多個全局配置倉庫嗎？
A: 技術上可以，但不建議。建議使用一個全局倉庫 + 各專案的本地配置。

## 🔗 相關資源

- [Claude Code 官方文件](https://github.com/anthropics/claude-code)
- [Skill 撰寫指南](skills/README.md)（如果有的話）

## 📧 聯絡

有問題或建議？歡迎提交 Issue 或 PR！
